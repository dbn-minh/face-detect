import express from 'express';
import TeacherController from '../controllers/teacherController.js';

const router = express.Router();

// router.get('/details/:parent_id', TeacherController.getParentDetails);
// router.get('/notification/:parent_id', TeacherController.getNotifications);
// // router.get('/tracking/:parentID', ParentController.getBusTracking);
// router.get('/profile/:parent_id', TeacherController.getParentProfile);
// router.put('/profile/:parent_id', TeacherController.updateParentProfile);
// router.post('/register/:parent_id', TeacherController.registerStudent);

export default router;
